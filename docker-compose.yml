version: "3.8"

services:
  stock-tracker:
    build: .
    container_name: stock-tracker
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - stock-data:/app/data
    environment:
      - PORT=3001
      - NODE_ENV=production

volumes:
  stock-data:
    driver: local
